<h1>投稿一覧</h1>
	<% @lists.each do |list| %>
		<p>タイトル</p>
		<%= link_to "#{list.title}", todolist_path(list.id)%>
		<p>画像</p>
		<%= attachment_image_tag list, :image, size: "100x100" %>

	<% end %>

	<%= link_to "新規作成", todolists_new_path %>